#include <iostream>
#include <stdio.h>
#include <stdlib.h>
// we have to return th4 median at at instant whilr taking struam of inputs
// median ->avaerage of two center most elements when arraged in ascending order
//pass first have of array into min heap and rest half to max heap 
//return avg of top vales

//size of two heaps must not differe more than 1
//at ech point of inserting  if the element is greater then th element top in the min heap addred it will be added to max heap else min heap 
//now if size diff >2 shuffle the elements b/w the two heaps

//add first element return median chek which one is empty if one is empty retirn the sencond

using namespace std;
int main()
{
    return 0;
}